# Exemplo de variáveis de ambiente (MVP)
# Copie para .env.local e preencha os valores reais (não commitá-los).

# Dica: para provedores gerenciados (Supabase/Neon/Render), use ?sslmode=require no DATABASE_URL
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
MP_ACCESS_TOKEN=TEST-XXXXXXXXXXXXXXXXXXXXXXXXXXXX
MP_WEBHOOK_SECRET=changeme
NEXT_PUBLIC_META_PIXEL_ID=
SITE_URL=http://localhost:3000
NEXT_PUBLIC_ALLOW_FORCE_PAID=0 # defina como 1 apenas em dev para habilitar botão [DEV] no checkout
